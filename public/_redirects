# Cloudflare Pages Redirects
# Note: Cloudflare Pages does not support proxy (200) redirects to external URLs
# External API calls must be made directly from the client to Supabase

# Non-www to www redirect (301 permanent)
https://garantieproremorque.com/* https://www.garantieproremorque.com/:splat 301

# SPA fallback - all non-file routes serve index.html
/*    /index.html   200
